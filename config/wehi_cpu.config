process {
    withLabel:train {
        conda = "${projectDir}/envs/environment.yml"
        cpus = 56
        memory = "100 GB"
    }
    withLabel:report {
        conda = "${projectDir}/envs/environment.yml"
    }
}

params {
    classifier = "Xgboost"
}

conda.useMicromamba = true